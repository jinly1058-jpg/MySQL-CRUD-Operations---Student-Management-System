# MySQL-CRUD-Operations---Student-Management-System
This project demonstrates basic MySQL CRUD (Create, Read, Update, Delete) operations on a Student table. It includes creating a table, inserting records, retrieving data, updating records, deleting records, and dropping the table.
